
export default function Qualification({ number, content }) {
  // if (startYear) {

  // }
  return (
    <div className="qual">
      <p className="qual-number">{number}</p>
      <p className="qual-content">{content}</p>
    </div>
  )
};