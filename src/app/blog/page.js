// 'use client'
// import { useEffect } from "react";
import { DateTime } from 'luxon';
import { getAllBlogs } from "@/database/database";

export default async function BlogSpot() {
  // let blogs = await getAllBlogs();
  // let newDate = new DateTime(blogs[0].blog_created);
  // return (
  //   <main id="blogspot">
  //     <img src={blogs[0].blog_thumbnail} alt={blogs[0].blog_title} />
  //     <p>this is the blog page</p>
  //     <p>{blogs[0].blog_title} {blogs[0].blog_content} {blogs[0].blog_created.toLocaleString(DateTime.DATE_MED)}</p>
  //   </main>
  // );
}